# R package check

`00check.log` is the final `R CMD check --as-cran --no-manual` log for the
version 0.1.0 source archive. The result was:

- 0 errors
- 0 warnings
- 1 note: `New submission`

The note is informational for a package that has not previously been submitted
to CRAN. It does not indicate a code, documentation, test, or installation
failure.
