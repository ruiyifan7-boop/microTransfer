# Direct sandbox optimization

`Figure6_extended_validation.pdf/png/svg` was updated with a restrained vector
header that states the study-held-out validation message before the four data
panels. The underlying panels and values were not changed. The PDF was assembled
with the original PDF page as a vector layer; the PNG was rendered from that PDF;
the SVG master was updated in parallel.

`Figure4_management_transferability.pdf/png/tiff` was also updated by removing the
three large grey facet strips in panel d and replacing them with small, plain taxon
labels. No data marks, model estimates, or statistical annotations were altered.

`Figure2_controlled_pH_responses.pdf/png` now has a compact interpretation band and
an explicit Low/Optimum/High colour key. `Figure3_pH_transferability.pdf/png/tiff`
now uses plain statistical labels for the two panel-C fits instead of grey facet
title strips. These are layout-only edits.

This is a layout-only change and does not alter any analysis or statistical result.

Figure 2 now includes explicit row labels for bacteria and fungi. Figure 5 was rebuilt
from the local vector core and cross-kingdom source PDFs rather than the earlier stacked
raster composite; the four panels retain vector text and marks, with uppercase panel
labels and the source-native analysis descriptors.

All six main figures were harmonized to an iMeta-style presentation: uppercase panel
labels, compact panel descriptors where they improve interpretation, explicit bacteria/
fungi row labels in Figure 2, a vector-native Figure 5 composite, and a restrained
Figure 6 header. The plotted values, statistical annotations, and underlying analyses
were not changed; these are presentation and layout edits only.

Figure 6 panel labels were repositioned in the source vector coordinate system:
A/B occupy the upper-panel margins, C occupies the inter-row whitespace, and D
occupies the whitespace above the heatmap. This avoids overlap with plotted marks,
count labels, and heatmap cells.

The final Figure 6 SVG keeps the panel labels as editable vector text. C is
aligned to the upper-left whitespace of the lower-left panel, and D is drawn
after the heatmap layer in the upper-left whitespace of the lower-right panel.
